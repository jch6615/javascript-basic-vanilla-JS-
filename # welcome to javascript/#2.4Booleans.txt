// boolean 
// boolean 에는 true 와 false 만이 존재.
// 컴퓨터가 0 과 1 만 인식하는 것 과 같음.
// ture & false 에는 "" 을 붙이지 않는다.

// JS의 데이터 타입중에는 존재하지 않음. 아무것도 없음. 을 나타내는 타입이 있다. ➤ null
// 변수에 값을 부여하지 않고 undefined 상태로 할 수도 있음.
// 이 경우에는 let 을 사용한다. 나중에 변경가능 하도록. const로는 정의 안됨.
const amIFat = null;
let something;
console.log(something, amIFat); 

//tips) Python 에서는...
// null 대신 None
// true 대신 True
// false 대신 False 를 사용한다.